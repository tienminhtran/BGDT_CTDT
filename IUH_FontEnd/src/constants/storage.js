// Các khóa (key) dùng để lưu dữ liệu trong localStorage.
// Gom về một nơi để tránh gõ chuỗi tay rải rác khắp code.
export const STORAGE_KEYS = {
  token: 'moodle_token',
  loginAt: 'moodle_login_at', // mốc thời gian đăng nhập (ms) để tính hết hạn phiên
}
